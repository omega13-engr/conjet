# Power Methodology

Conjet optimizes wakeups and energy-to-solution, not only wall time. The first
governor implementation emits deterministic resource policies; later phases
will attach `powermetrics`, VM stats, cgroup stats, and sync queue metrics.
