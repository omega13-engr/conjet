# Workloads

Planned workloads follow `CONJET_IMPLEMENTATION_PLAN.md`: empty container start,
Compose startup, package installs, hot reload, image build, database volume
writes, and idle power samples.
